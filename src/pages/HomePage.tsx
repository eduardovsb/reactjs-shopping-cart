const HomePage = () => {
    return (
        <>
            <h2>HomePage</h2>
            <p>Basic Shopping Cart project to pratice and demonstrate ReactJS + TypeScript + Redux + SASS.</p>
        </>
    );
};

export default HomePage;
